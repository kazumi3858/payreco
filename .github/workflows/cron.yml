name: daily-cron
on:
  schedule:
    - cron: '30 1,2 * * *'

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -X GET "https://payreco.fly.dev/api/v1/exchange_rates"
